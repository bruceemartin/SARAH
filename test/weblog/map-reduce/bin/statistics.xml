<configuration>

    <!-- Input Data Set -->
    <property>
        <!-- The number of records in the input data set. -->
    <name>sarah.input.number.records</name>
    <value>4477843</value>
    </property>

    <!-- Generated Sample -->
    <property>
        <!-- The seed used to generate the random sample. -->
    <name>sarah.sample.seed</name>
    <value>10367</value>
    </property>
    <property>
        <!-- The desired fraction of records included in the generated random sample. -->
    <name>sarah.target.sample.fraction</name>
    <value>.01</value>
    </property>
    <property>
        <!-- Fraction of the records actually included in the generated random sample. -->
    <name>sarah.sample.fraction</name>
    <value>0.01015109283643933</value>
    </property>
    <property>
        <!-- Hadoop file output format of the generated random sample. -->
    <name>sarah.sample.outputformat.class</name>
    <value>org.apache.hadoop.mapreduce.lib.output.TextOutputFormat</value>
    </property>
    <property>
        <!-- The class that implements the key in the generated random sample. -->
    <name>sarah.sample.output.key.class</name>
    <value>org.apache.hadoop.io.NullWritable</value>
    </property>
    <property>
        <!-- The class that implements the record in the generated random sample. -->
    <name>sarah.sample.output.value.class</name>
    <value>org.apache.hadoop.io.Text</value>
    </property>
    <property>
        <!-- The number of records in the generated random sample. -->
    <name>sarah.sample.number.records</name>
    <value>45455</value>
    </property>
    <property>
        <!-- The size of the smallest record in the generated sample. -->
    <name>sarah.sample.minsize.record</name>
    <value>null</value>
    </property>
    <property>
        <!-- The size of the largest record in the generated sample. -->
    <name>sarah.sample.maxsize.record</name>
    <value>null</value>
    </property>

    <!-- Functions -->
    <property>
        <!-- The functions applied to the generated sample. -->
    <name>sarah.functions</name>
    <value>EveningFilter,IPFromLog</value>
    </property>

    <!-- EveningFilter -->
    <property>
        <!-- The class that implements the EveningFilter function. -->
    <name>sarah.EveningFilter.class</name>
    <value>sarah.mapreduce.test.EveningFilterMapper</value>
    </property>
    <property>
        <!-- The class that implements the key output by the EveningFilter function. -->
    <name>sarah.EveningFilter.output.key.class</name>
    <value>org.apache.hadoop.io.Text</value>
    </property>

    <!-- IPFromLog -->
    <property>
        <!-- The class that implements the IPFromLog function. -->
    <name>sarah.IPFromLog.class</name>
    <value>sarah.mapreduce.test.IPFromLog</value>
    </property>
    <property>
        <!-- The class that implements the key output by the IPFromLog function. -->
    <name>sarah.IPFromLog.output.key.class</name>
    <value>org.apache.hadoop.io.Text</value>
    </property>

    <!-- Functions Applied to Sample -->

    <!-- EveningFilter -->
    <property>
        <!-- The number of records resulting from applying EveningFilter to the generated random sample. -->
    <name>sarah.sample.EveningFilter.number.records</name>
    <value>15025</value>
    </property>
    <property>
        <!-- The number of distinct keys resulting from applying EveningFilter to the generated random sample. -->
    <name>sarah.sample.EveningFilter.number.keys</name>
    <value>11</value>
    </property>
    <property>
        <!-- The size of the smallest record resulting from applying EveningFilter to the generated sample. -->
    <name>sarah.sample.EveningFilter.minsize.record</name>
    <value>0</value>
    </property>
    <property>
        <!-- The size of the largest record resulting from applying EveningFilter to the generated sample. -->
    <name>sarah.sample.EveningFilter.maxsize.record</name>
    <value>0</value>
    </property>
    <property>
        <!-- The total size of the records resulting from applying EveningFilter to the generated sample. -->
    <name>sarah.sample.EveningFilter.totalsize.records</name>
    <value>0</value>
    </property>
    <property>
        <!-- The mean record size resulting from applying EveningFilter to the generated random sample. -->
    <name>sarah.sample.EveningFilter.mean.size.record</name>
    <value>0</value>
    </property>
    <property>
        <!-- The size of the record at the 25% percentile resulting from applying EveningFilter to the generated sample. -->
    <name>sarah.sample.EveningFilter.percentile25.size.record</name>
    <value>0</value>
    </property>
    <property>
        <!-- The size of the record at the 50% percentile resulting from applying EveningFilter to the generated sample. -->
    <name>sarah.sample.EveningFilter.percentile50.size.record</name>
    <value>0</value>
    </property>
    <property>
        <!-- The size of the record at the 75% percentile resulting from applying EveningFilter to the generated sample. -->
    <name>sarah.sample.EveningFilter.percentile75.size.records</name>
    <value>0</value>
    </property>

    <!-- IPFromLog -->
    <property>
        <!-- The number of records resulting from applying IPFromLog to the generated random sample. -->
    <name>sarah.sample.IPFromLog.number.records</name>
    <value>45455</value>
    </property>
    <property>
        <!-- The number of distinct keys resulting from applying IPFromLog to the generated random sample. -->
    <name>sarah.sample.IPFromLog.number.keys</name>
    <value>20373</value>
    </property>
    <property>
        <!-- The size of the smallest record resulting from applying IPFromLog to the generated sample. -->
    <name>sarah.sample.IPFromLog.minsize.record</name>
    <value>0</value>
    </property>
    <property>
        <!-- The size of the largest record resulting from applying IPFromLog to the generated sample. -->
    <name>sarah.sample.IPFromLog.maxsize.record</name>
    <value>0</value>
    </property>
    <property>
        <!-- The total size of the records resulting from applying IPFromLog to the generated sample. -->
    <name>sarah.sample.IPFromLog.totalsize.records</name>
    <value>0</value>
    </property>
    <property>
        <!-- The mean record size resulting from applying IPFromLog to the generated random sample. -->
    <name>sarah.sample.IPFromLog.mean.size.record</name>
    <value>0</value>
    </property>
    <property>
        <!-- The size of the record at the 25% percentile resulting from applying IPFromLog to the generated sample. -->
    <name>sarah.sample.IPFromLog.percentile25.size.record</name>
    <value>0</value>
    </property>
    <property>
        <!-- The size of the record at the 50% percentile resulting from applying IPFromLog to the generated sample. -->
    <name>sarah.sample.IPFromLog.percentile50.size.record</name>
    <value>0</value>
    </property>
    <property>
        <!-- The size of the record at the 75% percentile resulting from applying IPFromLog to the generated sample. -->
    <name>sarah.sample.IPFromLog.percentile75.size.records</name>
    <value>0</value>
    </property>
</configuration>
